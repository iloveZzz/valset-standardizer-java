-- 修复历史运行库中 TP_MATCH_RULES 数值列精度不足导致的 ORA-01438。
-- 当前应用使用雪花 ID，TP_MATCH_RULES.ID 必须能容纳 19 位左右的 Long 值。

ALTER TABLE "TP_MATCH_RULES" MODIFY ("ID" NUMBER(20));
ALTER TABLE "TP_MATCH_RULES" MODIFY ("IS_VALID" NUMBER(4));
